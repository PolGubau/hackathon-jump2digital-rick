// File to store all the API endpoints

// Base URL for the API
export const API_URL = "https://rickandmortyapi.com/api/";

// Enum for the API endpoints
export const API = {
	characters: API_URL + "character",
};
